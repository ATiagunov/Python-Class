#class TF_IDF:
   # def __init__(self):
       # if idf_fir exists()
      #  self.idf = load()
       # else&
       # self.idf = count.idf
   # def count_tf_idf(text):
        #tf_idf_dict[word]=tf


    #считаем один раз второй р

    #словарь слово-значение idf
    #1 публичный метод
    #роверка существвоать==ние os path exists(path to )
